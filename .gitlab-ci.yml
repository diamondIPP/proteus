image: gitlab-registry.cern.ch/ci-tools/ci-worker:slc6

stages:
  - build
  - test

# global cache that should be shared among all jobs
cache:
  key: data
  paths:
    - test/raw/

# setup default build environment w/ minimal version requirements
# run debug build by default
before_script:
  - source /cvmfs/sft.cern.ch/lcg/views/LCG_88/x86_64-slc6-gcc49-opt/setup.sh
  - export "PATH=${PWD}/install-slc6-dbg/bin${PATH:+:}${PATH}"
  - export "LD_LIBRARY_PATH=${PWD}/install-slc6-dbg/lib${LD_LIBRARY_PATH:+:}${LD_LIBRARY_PATH}"

check-format:
  # clang-format on ci yields different output from local installation
  allow_failure: true
  stage: build
  tags:
    - cvmfs
  script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build . -- check-format

build-slc6-dbg:
  stage: build
  tags:
    - cvmfs
  variables:
    # Build type=Debug: activates compilation of asserts and
    # includes debug symbols (See issue #43)
    CMAKE_ARGS: -GNinja -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=${CI_PROJECT_DIR}/install-slc6-dbg
  script:
    - mkdir build_external && cd build_external
    - cmake ${CMAKE_ARGS} ../ci/external
    # external tools are installed already during build step
    - cmake --build .
    - mkdir ../build && cd ../build
    - cmake ${CMAKE_ARGS} -DPROTEUS_USE_EUDAQ=on ..
    - cmake --build . -- install
  artifacts:
    paths:
      - install-slc6-dbg

build-slc6-opt:
  stage: build
  tags:
    - cvmfs
  variables:
    CMAKE_ARGS: -GNinja -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=${CI_PROJECT_DIR}/install-slc6-opt
  script:
    - mkdir build-external && cd build-external
    - cmake ${CMAKE_ARGS} ../ci/external
    # external tools are installed already during build step
    - cmake --build .
    - mkdir ../build && cd ../build
    - cmake ${CMAKE_ARGS} -DPROTEUS_USE_EUDAQ=on ..
    - cmake --build .

build-cc7-dbg:
  image: gitlab-registry.cern.ch/ci-tools/ci-worker:cc7
  stage: build
  tags:
    - cvmfs
  before_script: []
  script:
    - source /cvmfs/sft.cern.ch/lcg/views/LCG_88/x86_64-centos7-gcc62-opt/setup.sh
    - mkdir build && cd build
    - cmake -GNinja -DCMAKE_BUILD_TYPE=Debug ..
    - cmake --build .

build-cc7-opt:
  image: gitlab-registry.cern.ch/ci-tools/ci-worker:cc7
  stage: build
  tags:
    - cvmfs
  before_script: []
  script:
    - source /cvmfs/sft.cern.ch/lcg/views/LCG_88/x86_64-centos7-gcc62-opt/setup.sh
    - mkdir build && cd build
    - cmake -GNinja -DCMAKE_BUILD_TYPE=Release ..
    - cmake --build .

doc:
  image: gitlab-registry.cern.ch/unige-fei4tel/proteus:cc7
  stage: build
  before_script: []
  script:
    - mkdir build && cd build
    - cmake -DPROTEUS_ENABLE_DOC=on ..
    - cmake --build . -- doc
  artifacts:
    paths:
      - build/doc/html

check_configs:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./check_configs.sh

combine:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./download_data.py
    - ./run_combine.sh

run000875_noisescan:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./download_data.py
    - ./run_tel_noisescan.sh 875 -n 100000 --no-progress
    - ./run_dut0_noisescan.sh 875 -n 100000 --no-progress

run000875_align:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./download_data.py
    - ./run_tel_align.sh 875 -n 20000 --no-progress

run000875_analysis:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./download_data.py
    - ./run_track.sh 875 -n 100000 --no-progress
    - ./run_match.sh 875 --no-progress

run001066_noisescan:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./download_data.py
    - ./run_tel_noisescan.sh 1066 -n 100000 --no-progress
    - ./run_dut0_noisescan.sh 1066 -n 100000 --no-progress

run001066_align:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./download_data.py
    - ./run_tel_align.sh 1066 -n 20000 --no-progress
    - ./run_dut_align.sh 1066 -n 20000 --no-progress

run001066_analysis:
  stage: test
  tags:
    - cvmfs
  script:
    - cd test
    - ./download_data.py
    - ./run_track.sh 1066 -n 100000 --no-progress
    - ./run_match.sh 1066 --no-progress
