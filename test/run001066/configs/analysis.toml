[align]
# sensors that should be aligned
sensor_ids = [1, 2, 3, 4]
# sensors that should be ignored
ignore_ids = [6, 7]
distance_sigma_max = 4.0
reduced_chi2_max = 5.0
method = "residuals" # local_chi2, global_chi2

[track]
sensor_ids = [0, 1, 2, 3, 4, 5]
num_points_min = 4
distance_sigma_max = 10.
